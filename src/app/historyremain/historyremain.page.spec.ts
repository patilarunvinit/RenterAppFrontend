import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HistoryremainPage } from './historyremain.page';

describe('HistoryremainPage', () => {
  let component: HistoryremainPage;
  let fixture: ComponentFixture<HistoryremainPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HistoryremainPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
